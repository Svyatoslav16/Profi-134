{
  "name": "profi-134",
  "private": true,
  "workspaces": [
    "producer-service",
    "consumer-service"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "start:dev": "concurrently -n producer,consumer -c blue,green \"npm run start:dev -w producer-service\" \"npm run start:dev -w consumer-service\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
